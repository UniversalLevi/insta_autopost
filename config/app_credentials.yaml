# Instagram App Credentials
# Keep this file secure and never commit to version control

instagram:
  app_id: "1627803201547308"
  app_secret: "581a880923cfbfd81ce8d8facac509b2"
  account_id: ""  # Instagram Business Account ID (will be auto-detected)

# Note: These credentials can be used for:
# - Token refresh (converting short-lived to long-lived tokens)
# - Token validation
# - OAuth flows

# Security: Add this file to .gitignore
# Meta Login: kanishkkumawat.rex@gmail.com (password stored securely, not in this file)